[project]
name = "stock-auto-backtest"
version = "0.1.0"
description = "Position management engine for backtesting and live trading"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = ["pytest"]
